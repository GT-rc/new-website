import React from 'react';
import ReactDOM from 'react-dom';

// Make a page with components! It's fun times.  :)
const App = () => {
	return <div>Hiya, buddy.</div>;
};

// Render it
ReactDOM.render(<App />, document.getElementById('root'));
